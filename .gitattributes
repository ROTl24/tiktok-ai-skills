# Keep text files stable across Windows/macOS/Linux so Chinese content does not get mangled.
# All text files use LF line endings; save files as UTF-8 (no BOM).
* text=auto
*.md text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.json text eol=lf
